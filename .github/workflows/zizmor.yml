name: zizmor

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "42 18 * * 5"
  workflow_dispatch:

permissions: {}

concurrency:
  group: zizmor-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: GitHub Actions Security Analysis
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          version-file: "pyproject.toml"
          python-version: "3.14"
      - name: Fail on workflow findings
        id: audit
        env:
          ZIZMOR_GITHUB_TOKEN: ${{ github.token }}
        run: uv run --locked research-repo-tools zizmor check --require-online
      - name: Produce SARIF even when the finding gate fails
        id: sarif
        if: ${{ !cancelled() && steps.audit.outcome != 'skipped' }}
        env:
          ZIZMOR_GITHUB_TOKEN: ${{ github.token }}
        run: uv run --locked research-repo-tools zizmor check --require-online --format sarif > "$RUNNER_TEMP/zizmor.sarif"
      - name: Upload security findings
        if: ${{ !cancelled() && steps.sarif.outcome == 'success' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && github.actor != 'dependabot[bot]' }}
        uses: github/codeql-action/upload-sarif@1c5b675653bb5c22dbe9b12b556ec555138e09fd # v4.38.1
        with:
          sarif_file: ${{ runner.temp }}/zizmor.sarif
          category: zizmor
